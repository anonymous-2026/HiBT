# Third-Party Notices

This repository is self-contained for execution and does not ship full external
codebases inside the source tree.

## Distribution note

This repository does not bundle full external codebases. The runnable path uses
repository-owned code under `artifact/` and `planner/`.

Model weights and large checkpoints are not bundled. Those are treated as
runtime assets and should be downloaded or reproduced separately.
